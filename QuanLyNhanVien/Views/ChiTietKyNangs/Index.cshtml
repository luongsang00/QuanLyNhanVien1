@*@model IEnumerable<QuanLyNhanVien.Models.ChiTietKyNang>*@
@model IEnumerable<QuanLyNhanVien.Controllers.ChiTietKyNangsController.Kynang>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>Tên Nhân Viên</th>
            <th>Tên Kỹ Năng</th>
        </tr>
        
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.TenNhanVien)
                @*@Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.TenKyNang)
                </td>
            
        </tr>
}
    </tbody>
</table>
